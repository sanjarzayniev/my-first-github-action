name: challange

on: push

jobs: 
        job1:
                name: First job
                runs-on: ubuntu-latest
                steps:
                        - name: Print the date
                          run: date
        job2: 
                name: Second job
                runs-on: windows-latest
                steps: 
                        - name: Print the date
                          run: date /t
        job3:
                name: Third job
                runs-on: macos-latest
                steps:
                        - name: Print the date
                          run: Date
        job4:
                name: Fourth job
                runs-on: ubuntu-latest
                steps:
                        - name: Print the date in second ubuntu
                          run: date
